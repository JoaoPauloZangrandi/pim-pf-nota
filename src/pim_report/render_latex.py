"""render_latex.py — renderização Jinja2 -> .tex -> PDF (build determinístico).

Preenche templates/nota.tex.j2, compila via subprocess (latexmk -pdf; tectonic se disponível),
captura stdout/stderr e o .log; em falha salva o log e levanta LatexCompilationError com contexto.

A ser implementado na Etapa 5.
"""

# Placeholder — implementação na Etapa 5.
